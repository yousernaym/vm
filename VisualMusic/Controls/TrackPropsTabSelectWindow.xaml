<mah:MetroWindow x:Class="VisualMusic.Controls.TrackPropsTabSelectWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 Title="Select properties"
                 ResizeMode="NoResize"
                 SizeToContent="WidthAndHeight"
                 WindowStartupLocation="CenterOwner"
                 ShowMinButton="False"
                 ShowMaxRestoreButton="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Include these property tabs:"/>

        <StackPanel Grid.Row="2" MinWidth="180">
            <CheckBox x:Name="cbStyle"    Content="Style"    Margin="0,2"/>
            <CheckBox x:Name="cbMaterial" Content="Material" Margin="0,2"/>
            <CheckBox x:Name="cbLight"    Content="Light"    Margin="0,2"/>
            <CheckBox x:Name="cbSpatial"  Content="Spatial"  Margin="0,2"/>
            <CheckBox x:Name="cbAudio"    Content="Audio"    Margin="0,2"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK"     Width="80" IsDefault="True" Click="Ok_Click"     Margin="0,0,8,0"/>
            <Button Content="Cancel" Width="80" IsCancel="True"  Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</mah:MetroWindow>
